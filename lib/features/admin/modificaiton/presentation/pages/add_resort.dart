import 'package:flutter/material.dart';

class AddResort extends StatelessWidget {
  const AddResort({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
